services:
  frontend:
    build: .
    container_name: agendai-frontend
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - VITE_BASE_URL=/api
